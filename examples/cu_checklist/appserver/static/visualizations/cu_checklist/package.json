{
  "name": "cu-checklist-viz",
  "version": "1.0.0",
  "description": "Agentic Usage — ingest setup checklist with self-hiding banner mode (DOM custom visualization)",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch"
  },
  "devDependencies": {
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.4"
  }
}
